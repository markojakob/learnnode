<script setup>
import { ref } from 'vue';

defineProps(['items']);
let active = ref();
</script>

<template>
    <div class="tabs">
        <ul>
            <li v-for="(item, key) in items" :class="{'is-active' : key == active}" @click="active=key"><a>{{item}}</a></li>
        </ul>
    </div>
</template>